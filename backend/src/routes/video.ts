import express from 'express';

const router = express.Router();

// Placeholder routes - to be implemented
router.post('/analyze', (req, res) => {
  res.json({ message: 'Video analysis - to be implemented' });
});

export default router;
