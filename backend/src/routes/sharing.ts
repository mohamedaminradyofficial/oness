import express from 'express';

const router = express.Router();

// Placeholder routes - to be implemented
router.post('/', (req, res) => {
  res.json({ message: 'Sharing routes - to be implemented' });
});

export default router;
