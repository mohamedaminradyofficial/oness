import express from 'express';

const router = express.Router();

// Placeholder routes - to be implemented
router.post('/analyze', (req, res) => {
  res.json({ message: 'Image analysis - to be implemented' });
});

export default router;
